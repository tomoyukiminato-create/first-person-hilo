# First Person HiLo PWA — 無限デッキ・ライブ判定版

判定結果の表示を完全に動的化しました。

カードまたは倍率を変更すると、その場で以下が更新されます。

- 判定結果
- 判定カードの色
- LOW側 / HIGH側 / NO BET / 同率
- 判定理由
- 「LOW側を選択」「HIGH側を選択」「BETしない」などのアクション表示

初期表示は「入力待ち」です。固定のHIGH/LOW表示はありません。

確率は無限デッキモデルで、2〜Aの13ランクを各1/13として計算します。
各ドローは独立で、過去カードは確率に影響しません。
